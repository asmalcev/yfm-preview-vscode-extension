# yfm-preview-vscode-extension

Extension for preview Yandex Flowered Markdown ([docs](https://diplodoc.com/docs/ru/index-yfm))

Install from vsix:
```bash
code --install-extension yfm-preview-vscode-extension.vsix
```